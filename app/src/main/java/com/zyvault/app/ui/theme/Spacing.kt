package com.zyvault.app.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Consistent spacing tokens used throughout the app.
 */
object Spacing {
    val screenPadding = 16.dp    // Horizontal padding from screen edges
    val cardPadding = 16.dp      // Internal card padding
    val cardGap = 8.dp           // Gap between stacked cards
    val sectionGap = 24.dp       // Gap between major sections
    val innerGap = 12.dp         // Gap between elements inside a card
    val tinyGap = 4.dp           // Minimal spacing
    val pillGap = 8.dp           // Between filter pills
    val iconSize = 44.dp         // Standard icon container
    val iconSizeSmall = 22.dp    // Icon inside container
    val cardRadius = 12.dp       // Card corner radius
    val pillRadius = 20.dp       // Pill/badge corner radius
    val buttonRadius = 12.dp     // Button corner radius
    val buttonRadiusFull = 100.dp // Fully rounded buttons
    val statusBarWidth = 4.dp    // Left accent bar on cards
    val dividerHeight = 1.dp     // Separator lines
    val topBarHeight = 56.dp     // Top bar consistent height
}
